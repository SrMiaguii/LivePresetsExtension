meson setup build
meson setup --reconfigure build
meson compile livepresets_x64 -C builddir

git tag v0.12.0
git push origin v0.12.0

git add liblpe/ui/LivePresetsTreeAdapter.cpp
git commit -m "Fix tree adapter: add ONLINE param, collapse by default"
git push

git add .
git commit -m "About window: show version and date, English labels"
git push